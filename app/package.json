{
  "name": "plate-app",
  "version": "1.0.0",
  "private": true,
  "description": "iOS app shell for Plate (plate-site) built with Capacitor",
  "scripts": {
    "sync": "node fetch-site.js && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
